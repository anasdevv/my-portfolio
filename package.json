{
  "name": "portfolio-pro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "client-side": "npm start --prefix client-side",
    "dev": "concurrently \"npm run  server\" \"npm run client-side\"",
    "railway-postbuild": "npm run build --prefix client-side && npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemailer": "^6.9.1"
  }
}
